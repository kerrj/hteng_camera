__pycache__/
**out/
*.mp4
Camera/*
*.pyc
native/build/
*.o
*.egg-info/
build/
dist/
.venv/
venv/
*.png
snap_*.png
.DS_Store

# Calibration: publish the JSONs in calibrations/ (tracked, image-free), but
# keep the raw-image provenance bundles (session folders) out of git.
calib_session_*/
wb_measurement_*

# WiLoR hand-pose outputs (large, regenerable on chungus)
data_processing/out/

# Fast-FoundationStereo model weights (~900 MB, fetched via install script)
data_processing/weights/

# Raw stereo recordings (large: videos + derived/ pipeline outputs) — copied in,
# regenerable, not tracked.
/long-test1/
/long-test2/
/testimu/
